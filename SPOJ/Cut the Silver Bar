#include<stdio.h>
int main()
{int n;
 while(1)
  { scanf("%d",&n);
     if(n==0)
      break;
      int count=0;
     while(n>0)
      {n=n/2;
       count++;
      }
       printf("%d\n",count-1);
  }
   return 0;
}
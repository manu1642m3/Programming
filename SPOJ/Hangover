#include<stdio.h>
#include<stdlib.h>
int main()
{float i,j;
 float t;
 while(1)
  {i=2;j=-1;
      scanf("%f",&t);
   if(t==0)
    exit(0);
   while(t>=j)
    {j=1/i;
     t=t-j;
     if(t<0)
      i--;
     i++;
    }
    int m=i;
    printf("\n%d card(s)",m-1);
    
  }
}
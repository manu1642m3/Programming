#include<stdio.h>
#define ll long long
int main()
{ ll t;
  scanf("%llu",&t);
   while(t--)
    { ll max1=-1,max2=-1,ng,nm,c;
      scanf("%llu%llu",&ng,&nm);
      
      for(ll i=0;i<ng;i++)
       { scanf("%llu",&c);
         if(c>max1)
          max1=c;
       }
        
        
       
       for(ll i=0;i<nm;i++)
        {scanf("%llu",&c);
         if(c>max2)
          max2=c;
        }
        
        if(max1>=max2)
         printf("Godzilla\n");
          else 
          printf("MechaGodzilla\n");
    }
    return(0);
}